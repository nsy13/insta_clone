<%= form_for(comment, url: comments_path) do |f| %>
  <%= f.label :comment, "コメント" %>
  <%= f.text_area :content, class: "form-control", placeholder: "コメントを入力する" %>
  <%= hidden_field_tag 'comment[post_id]', post.id %>

  <%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>