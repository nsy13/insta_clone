<% provide(:title, "パスワードのリセット")%>

<div class="row">
  <div class="col-md-8 col-md-offset-4">
    <h2>パスワードを忘れた方は</h2>
    <p>登録されたメールアドレス宛にメールをお送りします</p>
    <p>メールに記載された手順に従い、パスワードを変更してください</p>

    <%= form_for(resource, class: "form-group", as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス", class: "form-control" %>
      </div>

      <div class="actions">
        <%= f.submit "メールを送信する", class: "btn btn-primary" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
